import { View, Text } from "react-native";

export default function HotPlacesTab() {
  return (
    <View style={{ flex: 1, padding: 20, alignItems: "center" }}>
      <Text>인기 장소 탭</Text>
      {/* 여기에 인기 장소 관련 컴포넌트나 내용을 추가하세요 */}
    </View>
  );
}
